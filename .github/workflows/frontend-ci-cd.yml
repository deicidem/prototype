name: Frontend CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-root:
      name: Install, Lint, Test, and Build
      runs-on: ubuntu-latest

      steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Setup Node.js
          uses: actions/setup-node@v4
          with:
            node-version: 20
            cache: 'npm'
            cache-dependency-path: package-lock.json

        - name: Install dependencies
          run: npm ci
          working-directory: apps/root

        - name: Run Linter
          run: npm run lint
          working-directory: apps/root

        - name: Run Tests
          run: npm run test
          working-directory: apps/root

        - name: Build project
          run: npm run build
          working-directory: apps/root

  deploy-root:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/root/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/root

      - name: Build project
        run: npm run build
        working-directory: apps/root

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: apps/root/dist

  build-catalog:
    name: Install, Lint, Test, and Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/catalog

      - name: Run Linter
        run: npm run lint
        working-directory: apps/catalog

      - name: Run Tests
        run: npm run test
        working-directory: apps/catalog

      - name: Build project
        run: npm run build
        working-directory: apps/catalog

  deploy-catalog:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/catalog/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/catalog

      - name: Build project
        run: npm run build
        working-directory: apps/catalog

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: apps/catalog/dist

  build-profile:
    name: Install, Lint, Test, and Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/profile

      - name: Run Linter
        run: npm run lint
        working-directory: apps/profile

      - name: Run Tests
        run: npm run test
        working-directory: apps/profile

      - name: Build project
        run: npm run build
        working-directory: apps/profile

  deploy-profile:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/profile/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/profile

      - name: Build project
        run: npm run build
        working-directory: apps/profile

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: apps/profile/dist